<% if Person.any? %>
  <table>
    <thead>
      <tr>
        <th>#</th>
        <th>Name</th> 
        <th>Description</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
      <% Person.all.each do |person| %>
        <tr>
          <td><%= person.id %></td>
          <td><%= person.name %></td>
          <td><%= person.description %></td>
          <td>🐧 <%= link_to "Edit", edit_person_path(person.id)%></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  No people here! Use Rails console to add some.
<% end %>
<br />
<div style="float: right">
  💁 <%= link_to "Add new", new_person_path %>
</div>